// Placeholder validation schemas — integrate Joi or Zod when implementing business logic

export const validateLogin = (data) => data;
export const validateRegister = (data) => data;
export const validateProduct = (data) => data;
export const validateOrder = (data) => data;
